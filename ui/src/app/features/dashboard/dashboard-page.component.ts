import { ChangeDetectionStrategy, Component, OnInit, computed, inject } from '@angular/core';
import { Router } from '@angular/router';
import { firstValueFrom } from 'rxjs';
import { MatButtonModule } from '@angular/material/button';import { MatCardModule } from '@angular/material/card';import { MAT_DIALOG_DATA,MatDialog,MatDialogModule,MatDialogRef } from '@angular/material/dialog';import { MatFormFieldModule } from '@angular/material/form-field';import { MatInputModule } from '@angular/material/input';import { MatProgressBarModule } from '@angular/material/progress-bar';import { FormControl,ReactiveFormsModule,Validators } from '@angular/forms';
import { LearningStoreService } from '../../core/state/learning-store.service';import { ReviewSessionService } from '../../application/review/review-session.service';import { accuracy,getDueWords,hardWords,isActiveLeitnerWord,localDay,totalStats } from '../../domain/learning/learning-rules';
@Component({selector:'app-new-words-dialog',imports:[ReactiveFormsModule,MatDialogModule,MatButtonModule,MatFormFieldModule,MatInputModule],template:`<h2 mat-dialog-title>افزودن لغات جدید</h2><mat-dialog-content><p>{{data.available}} لغت هنوز وارد جعبه نشده است.</p><mat-form-field appearance="outline"><mat-label>تعداد لغت</mat-label><input matInput type="number" [formControl]="count" min="1" [max]="data.max"></mat-form-field></mat-dialog-content><mat-dialog-actions align="end"><button mat-button (click)="dialog.close()">انصراف</button><button mat-flat-button [disabled]="count.invalid" (click)="dialog.close(count.value)">افزودن و شروع آزمون</button></mat-dialog-actions>`,changeDetection:ChangeDetectionStrategy.OnPush})export class NewWordsDialogComponent{readonly data=inject<{available:number;max:number}>(MAT_DIALOG_DATA);readonly dialog=inject(MatDialogRef<NewWordsDialogComponent>);readonly count=new FormControl(10,{nonNullable:true,validators:[Validators.required,Validators.min(1),Validators.max(this.data.max)]});}
@Component({selector:'app-dashboard-page',imports:[MatButtonModule,MatCardModule,MatProgressBarModule],template:`@if(state();as s){<section class="page"><header><p>برنامهٔ امروز</p><h1>{{dueCount()? dueCount()+' مرور در انتظار توست':'برنامهٔ امروز کامل شد!'}}</h1><p>مرورهای موعددار و لغات جدیدت را در یک جلسهٔ کوتاه انجام بده.</p><div class="actions"><button mat-flat-button (click)="goReview()" [disabled]="!dueCount()">شروع مرور امروز</button><button mat-stroked-button (click)="startBoxOne()">تمرین آزاد خانهٔ ۱</button><button mat-stroked-button (click)="addNewWords()">افزودن لغات جدید</button></div></header><div class="stats">@for(card of statCards();track card.label){<mat-card appearance="outlined"><mat-card-subtitle>{{card.label}}</mat-card-subtitle><mat-card-title>{{card.value}}</mat-card-title><span>{{card.unit}}</span></mat-card>}</div><div class="grid"><mat-card appearance="outlined"><mat-card-header><mat-card-title>روند ۱۴ روزه</mat-card-title></mat-card-header><mat-card-content class="bars">@for(day of activity();track day.day){<div class="bar-column"><span>{{day.value}}</span><i [style.height.%]="day.height"></i><small>{{day.label}}</small></div>}</mat-card-content></mat-card><mat-card appearance="outlined"><mat-card-header><mat-card-title>وضعیت خانه‌ها</mat-card-title></mat-card-header><mat-card-content>@for(house of houses();track house.box){<button class="house-row" type="button" (click)="openHouse(house.box)"><span>خانهٔ {{house.box}}</span><mat-progress-bar mode="determinate" [value]="house.percent"/><strong>{{house.count}}</strong></button>}</mat-card-content></mat-card></div><mat-card appearance="outlined"><mat-card-header><mat-card-title>نیازمند توجه بیشتر</mat-card-title><span class="spacer"></span><button mat-button (click)="router.navigateByUrl('/words')">دیدن همه</button></mat-card-header><mat-card-content>@if(hardest().length){@for(word of hardest();track word.id){<div class="hard-word"><strong dir="ltr">{{word.term}}</strong><span>{{word.mistakes}} خطا · {{word.attempts}} تلاش</span></div>}}@else{<p>هنوز خطایی ثبت نشده است.</p>}</mat-card-content></mat-card></section>}`,styles:[`:host{display:block}.page{display:grid;gap:20px}.page>header{padding:32px;border-radius:28px;background:var(--mat-sys-primary-container);color:var(--mat-sys-on-primary-container)}header h1{font-size:clamp(26px,4vw,42px);margin:6px 0}.actions{display:flex;flex-wrap:wrap;gap:10px;margin-top:22px}.stats{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:14px}.stats mat-card{padding:18px}.stats mat-card-title{font-size:30px}.grid{display:grid;grid-template-columns:1.2fr .8fr;gap:18px}.grid mat-card{padding:18px}.bars{display:flex;height:170px;align-items:end;gap:6px}.bar-column{display:grid;flex:1;height:100%;align-content:end;text-align:center;gap:3px}.bar-column i{display:block;min-height:3px;background:var(--mat-sys-primary);border-radius:8px}.bar-column small{font-size:9px;color:var(--mat-sys-on-surface-variant)}.house-row{width:100%;display:grid;grid-template-columns:70px 1fr 30px;gap:10px;align-items:center;border:0;background:transparent;padding:10px;cursor:pointer;text-align:right;color:inherit;border-radius:12px}.house-row:hover{background:var(--mat-sys-surface-container)}.hard-word{display:flex;justify-content:space-between;padding:12px 0;border-bottom:1px solid var(--mat-sys-outline-variant)}.spacer{flex:1}@media(max-width:900px){.stats{grid-template-columns:1fr 1fr}.grid{grid-template-columns:1fr}}`],changeDetection:ChangeDetectionStrategy.OnPush})export class DashboardPageComponent implements OnInit{readonly store=inject(LearningStoreService);readonly review=inject(ReviewSessionService);readonly router=inject(Router);private readonly dialog=inject(MatDialog);readonly state=this.store.state;readonly dueCount=computed(()=>this.state()?getDueWords(this.state()!).length:0);readonly stats=computed(()=>this.state()?totalStats(this.state()!):{attempts:0,correct:0,mistakes:0,mastered:0,learning:0});readonly statCards=computed(()=>{const s=this.state();const stats=this.stats();const today=s?.daily[localDay()];return[{label:'موعد مرور',value:this.dueCount(),unit:'واژه'},{label:'دقت کلی',value:accuracy(stats.correct,stats.attempts)??'—',unit:'درصد'},{label:'یادگرفته‌شده',value:stats.mastered,unit:'خانهٔ ۵'},{label:'جدید امروز',value:today?.newAdded||0,unit:`از ${s?.settings.dailyNew||10}`}];});readonly hardest=computed(()=>this.state()?hardWords(this.state()!,3):[]);readonly houses=computed(()=>{const words=this.state()?.words.filter(isActiveLeitnerWord)||[];const counts=[1,2,3,4,5].map(box=>words.filter(word=>word.box===box).length);const max=Math.max(...counts,1);return counts.map((count,index)=>({box:index+1,count,percent:Math.round(count/max*100)}));});readonly activity=computed(()=>{const state=this.state();if(!state)return[];const fa=new Intl.DateTimeFormat('fa-IR',{month:'short',day:'numeric'});const days=Array.from({length:14},(_,i)=>{const d=new Date();d.setDate(d.getDate()-(13-i));return d;});const max=Math.max(...days.map(d=>state.daily[localDay(d)]?.correct||0),1);return days.map(d=>{const day=localDay(d),value=state.daily[day]?.correct||0;return{day,value,height:value?Math.max(8,Math.round(value/max*100)):2,label:fa.format(d)}});});async ngOnInit(){await this.store.initialize();}goReview(){this.router.navigateByUrl('/review');}async startBoxOne(){await this.router.navigate(['/review'],{queryParams:{mode:'box1'}});}openHouse(box:number){this.router.navigate(['/leitner-house',box]);}async addNewWords(){const s=this.store.snapshot();const available=s.words.filter(w=>w.box===0&&!w.introducedOn).sort((a,b)=>a.number-b.number);if(!available.length)return;const count=await firstValueFrom(this.dialog.open(NewWordsDialogComponent,{data:{available:available.length,max:Math.min(50,available.length)}}).afterClosed());if(!count)return;const result=await this.store.activateWords(available.slice(0,Number(count)),'home-selection');this.review.prepareNewWords(result.activated.map(w=>w.id));await this.router.navigate(['/review'],{queryParams:{mode:'new'}});}}
