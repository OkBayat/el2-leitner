import { ChangeDetectionStrategy, Component, OnInit, computed, inject, signal } from '@angular/core';
import { FormControl, FormGroup, ReactiveFormsModule, Validators } from '@angular/forms';
import { firstValueFrom } from 'rxjs';
import { MatButtonModule } from '@angular/material/button';import { MatCardModule } from '@angular/material/card';import { MatChipsModule } from '@angular/material/chips';import { MAT_DIALOG_DATA,MatDialog,MatDialogModule,MatDialogRef } from '@angular/material/dialog';import { MatFormFieldModule } from '@angular/material/form-field';import { MatInputModule } from '@angular/material/input';import { MatProgressBarModule } from '@angular/material/progress-bar';import { MatSelectModule } from '@angular/material/select';import { MatSnackBar } from '@angular/material/snack-bar';import { MatTableModule } from '@angular/material/table';
import { LibraryApiService } from '../../core/library/library-api.service';import { LibraryCollection,LibraryEntry } from '../../domain/learning/models';import { normalizeAnswer } from '../../domain/learning/learning-rules';import { ConfirmDialogComponent } from '../../shared/confirm-dialog/confirm-dialog.component';
function levelOf(collection:LibraryCollection):string{return String(collection.metadata?.['level']||'').trim()||'—'}function progressOf(collection:LibraryCollection){const total=Math.max(0,Number(collection.wordCount)||0),entered=Math.min(total,Math.max(0,Number(collection.leitnerWordCount)||0));return{total,entered,percent:total?Math.round(entered/total*100):0}}function slugify(value:string){return value.normalize('NFKD').replace(/[\u0300-\u036f]/gu,'').toLowerCase().replace(/['’]/gu,'').replace(/[^a-z0-9]+/gu,'-').replace(/^-+|-+$/gu,'').replace(/-{2,}/gu,'-').slice(0,160)}
interface CollectionDialogData{collection:LibraryCollection|null}interface CollectionPayload{title:string;slug:string;description:string;kind:string;visibility:string;status:string;metadata:Record<string,unknown>}
@Component({selector:'app-collection-editor',imports:[ReactiveFormsModule,MatDialogModule,MatButtonModule,MatFormFieldModule,MatInputModule,MatSelectModule],template:`<h2 mat-dialog-title>{{data.collection?'ویرایش مجموعه':'مجموعهٔ جدید'}}</h2><mat-dialog-content><form [formGroup]="form" class="form"><mat-form-field appearance="outline"><mat-label>عنوان</mat-label><input matInput formControlName="title"></mat-form-field><mat-form-field appearance="outline"><mat-label>Slug</mat-label><input matInput formControlName="slug" dir="ltr"></mat-form-field><mat-form-field appearance="outline"><mat-label>توضیح</mat-label><textarea matInput rows="3" formControlName="description"></textarea></mat-form-field><div class="row"><mat-form-field appearance="outline"><mat-label>نوع</mat-label><mat-select formControlName="kind"><mat-option value="book">کتاب</mat-option><mat-option value="exam">آزمون</mat-option><mat-option value="topic">موضوعی</mat-option><mat-option value="course">دوره</mat-option><mat-option value="personal">شخصی</mat-option></mat-select></mat-form-field><mat-form-field appearance="outline"><mat-label>سطح CEFR</mat-label><input matInput formControlName="level" placeholder="B1"></mat-form-field></div><div class="row"><mat-form-field appearance="outline"><mat-label>دسترسی</mat-label><mat-select formControlName="visibility"><mat-option value="public">عمومی</mat-option><mat-option value="unlisted">فقط با لینک</mat-option><mat-option value="private">خصوصی</mat-option></mat-select></mat-form-field><mat-form-field appearance="outline"><mat-label>وضعیت</mat-label><mat-select formControlName="status"><mat-option value="published">منتشرشده</mat-option><mat-option value="draft">پیش‌نویس</mat-option><mat-option value="archived">آرشیو</mat-option></mat-select></mat-form-field></div></form></mat-dialog-content><mat-dialog-actions align="end"><button mat-button (click)="dialog.close()">انصراف</button><button mat-flat-button [disabled]="form.invalid" (click)="save()">ذخیره</button></mat-dialog-actions>`,styles:[`.form{display:grid;min-width:min(75vw,650px);padding-top:8px}.row{display:grid;grid-template-columns:1fr 1fr;gap:10px}@media(max-width:600px){.row{grid-template-columns:1fr}}`],changeDetection:ChangeDetectionStrategy.OnPush})export class CollectionEditorComponent{readonly data=inject<CollectionDialogData>(MAT_DIALOG_DATA);readonly dialog=inject(MatDialogRef<CollectionEditorComponent>);readonly form=new FormGroup({title:new FormControl(this.data.collection?.title||'',{nonNullable:true,validators:[Validators.required]}),slug:new FormControl(this.data.collection?.slug||'',{nonNullable:true}),description:new FormControl(this.data.collection?.description||'',{nonNullable:true}),kind:new FormControl(this.data.collection?.kind||'book',{nonNullable:true}),level:new FormControl(levelOf(this.data.collection||({metadata:{}} as LibraryCollection))==='—'?'':levelOf(this.data.collection!),{nonNullable:true}),visibility:new FormControl(this.data.collection?.visibility||'public',{nonNullable:true}),status:new FormControl(this.data.collection?.status||'published',{nonNullable:true})});save(){const raw=this.form.getRawValue(),metadata={...(this.data.collection?.metadata||{})};if(raw.level)metadata['level']=raw.level;else delete metadata['level'];const payload:CollectionPayload={title:raw.title.trim(),slug:raw.slug.trim()||slugify(raw.title),description:raw.description.trim(),kind:raw.kind,visibility:raw.visibility,status:raw.status,metadata};this.dialog.close(payload)}}
interface ImportValue{text:string;mode:'replace'|'append'}
@Component({selector:'app-library-import-dialog',imports:[ReactiveFormsModule,MatDialogModule,MatButtonModule,MatFormFieldModule,MatInputModule,MatSelectModule],template:`<h2 mat-dialog-title>ورود واژه‌ها</h2><mat-dialog-content><input #file hidden type="file" accept=".md,.txt,text/plain,text/markdown" (change)="loadFile($event)"><button mat-stroked-button (click)="file.click()">انتخاب فایل MD / TXT</button><mat-form-field appearance="outline" class="wide"><mat-label>متن واژه‌ها</mat-label><textarea matInput rows="12" [formControl]="text"></textarea></mat-form-field><mat-form-field appearance="outline"><mat-label>روش ورود</mat-label><mat-select [formControl]="mode"><mat-option value="replace">جایگزینی محتوا</mat-option><mat-option value="append">افزودن به محتوا</mat-option></mat-select></mat-form-field></mat-dialog-content><mat-dialog-actions align="end"><button mat-button (click)="dialog.close()">انصراف</button><button mat-flat-button [disabled]="!text.value.trim()" (click)="dialog.close({text:text.value,mode:mode.value})">ورود</button></mat-dialog-actions>`,styles:[`.wide{display:block;width:min(75vw,700px);margin-top:14px}`],changeDetection:ChangeDetectionStrategy.OnPush})export class LibraryImportDialogComponent{readonly dialog=inject(MatDialogRef<LibraryImportDialogComponent>);readonly text=new FormControl('',{nonNullable:true});readonly mode=new FormControl<'replace'|'append'>('replace',{nonNullable:true});async loadFile(event:Event){const file=(event.target as HTMLInputElement).files?.[0];if(file)this.text.setValue(await file.text())}}
interface EntryDialogData{entry:LibraryEntry|null}
@Component({selector:'app-library-entry-dialog',imports:[ReactiveFormsModule,MatDialogModule,MatButtonModule,MatFormFieldModule,MatInputModule],template:`<h2 mat-dialog-title>{{data.entry?'ویرایش واژه':'واژهٔ جدید'}}</h2><mat-dialog-content><form [formGroup]="form" class="form"><mat-form-field appearance="outline"><mat-label>واژه</mat-label><input matInput formControlName="term" dir="ltr"></mat-form-field><mat-form-field appearance="outline"><mat-label>املای جایگزین با /</mat-label><input matInput formControlName="variants" dir="ltr"></mat-form-field><mat-form-field appearance="outline"><mat-label>بخش / درس</mat-label><input matInput formControlName="section"></mat-form-field><mat-form-field appearance="outline"><mat-label>یادداشت</mat-label><textarea matInput rows="3" formControlName="note"></textarea></mat-form-field></form></mat-dialog-content><mat-dialog-actions align="end"><button mat-button (click)="dialog.close()">انصراف</button><button mat-flat-button [disabled]="form.invalid" (click)="dialog.close(payload())">ذخیره</button></mat-dialog-actions>`,styles:[`.form{display:grid;min-width:min(70vw,520px);padding-top:8px}`],changeDetection:ChangeDetectionStrategy.OnPush})export class LibraryEntryDialogComponent{readonly data=inject<EntryDialogData>(MAT_DIALOG_DATA);readonly dialog=inject(MatDialogRef<LibraryEntryDialogComponent>);readonly form=new FormGroup({term:new FormControl(String(this.data.entry?.['term']||''),{nonNullable:true,validators:[Validators.required]}),variants:new FormControl(Array.isArray(this.data.entry?.['acceptedForms'])?(this.data.entry?.['acceptedForms'] as string[]).join(' / '):'',{nonNullable:true}),section:new FormControl(String(this.data.entry?.['sectionPath']||''),{nonNullable:true}),note:new FormControl(String(this.data.entry?.['note']||''),{nonNullable:true})});payload(){const r=this.form.getRawValue();return{term:r.term.trim(),acceptedForms:r.variants.split(/\s*\/\s*/u).map(x=>x.trim()).filter(Boolean),sectionPath:r.section.trim()||null,note:r.note.trim()||null}}}
@Component({selector:'app-library-detail-dialog',imports:[MatDialogModule,MatButtonModule,MatChipsModule,MatProgressBarModule,MatTableModule],template:`@if(collection();as c){<h2 mat-dialog-title>{{c.title}}</h2><mat-dialog-content class="detail"><p>{{c.description||'مجموعه‌ای از واژه‌ها برای مسیر یادگیری تو.'}}</p><mat-chip-set><mat-chip>{{kindLabel(c.kind)}}</mat-chip><mat-chip>{{level(c)}}</mat-chip><mat-chip>{{c.status}}</mat-chip><mat-chip>نسخه {{c.contentVersion}}</mat-chip></mat-chip-set><div class="progress"><span>{{progress(c).entered}} از {{progress(c).total}} واژه وارد لایتنر شده</span><mat-progress-bar mode="determinate" [value]="progress(c).percent"/></div><div class="actions"><button mat-flat-button (click)="toggleSubscription()">{{c.subscribed?'حذف از جعبه':'افزودن به جعبه'}}</button>@if(data.canManage){<button mat-stroked-button (click)="editCollection()">ویرایش مجموعه</button><button mat-stroked-button (click)="importEntries()">ورود فایل</button><button mat-stroked-button (click)="editEntry()">افزودن واژه</button>}</div>@if(c.entries?.length){<table mat-table [dataSource]="c.entries||[]"><ng-container matColumnDef="term"><th mat-header-cell *matHeaderCellDef>واژه</th><td mat-cell *matCellDef="let entry"><strong dir="ltr">{{entry.term}}</strong></td></ng-container><ng-container matColumnDef="section"><th mat-header-cell *matHeaderCellDef>بخش</th><td mat-cell *matCellDef="let entry">{{entry.sectionPath||'—'}}</td></ng-container><ng-container matColumnDef="actions"><th mat-header-cell *matHeaderCellDef></th><td mat-cell *matCellDef="let entry">@if(data.canManage){<button mat-icon-button (click)="editEntry(entry)">✎</button><button mat-icon-button (click)="removeEntry(entry)">×</button>}</td></ng-container><tr mat-header-row *matHeaderRowDef="entryColumns"></tr><tr mat-row *matRowDef="let row;columns:entryColumns"></tr></table>}@else{<p class="empty">واژه‌ای برای نمایش وجود ندارد.</p>}</mat-dialog-content><mat-dialog-actions><button mat-button (click)="dialog.close(changed)">بستن</button></mat-dialog-actions>}`,styles:[`.detail{min-width:min(82vw,850px);display:grid;gap:16px}.progress{display:grid;gap:8px}.actions{display:flex;flex-wrap:wrap;gap:8px}table{width:100%}.empty{padding:30px;text-align:center;color:var(--mat-sys-on-surface-variant)}`],changeDetection:ChangeDetectionStrategy.OnPush})export class LibraryDetailDialogComponent{readonly data=inject<{collection:LibraryCollection;canManage:boolean}>(MAT_DIALOG_DATA);readonly dialog=inject(MatDialogRef<LibraryDetailDialogComponent>);private readonly api=inject(LibraryApiService);private readonly dialogs=inject(MatDialog);private readonly snack=inject(MatSnackBar);readonly collection=signal(structuredClone(this.data.collection));readonly entryColumns=['term','section','actions'];changed=false;level=levelOf;progress=progressOf;kindLabel(kind:string){return({book:'کتاب',exam:'آزمون',topic:'موضوعی',course:'دوره',personal:'شخصی'} as Record<string,string>)[kind]||'مجموعه'}async reload(){const result=await this.api.get(this.collection().id);this.collection.set(result.collection)}async toggleSubscription(){const c=this.collection();if(c.subscribed)await this.api.unsubscribe(c.id);else await this.api.subscribe(c.id);this.changed=true;await this.reload()}async editCollection(){const value=await firstValueFrom(this.dialogs.open(CollectionEditorComponent,{data:{collection:this.collection()}}).afterClosed()) as CollectionPayload|undefined;if(!value)return;await this.api.update(this.collection().id,value);this.changed=true;await this.reload();this.snack.open('مشخصات مجموعه به‌روزرسانی شد.','باشه',{duration:2000})}async importEntries(){const value=await firstValueFrom(this.dialogs.open(LibraryImportDialogComponent).afterClosed()) as ImportValue|undefined;if(!value)return;await this.api.import(this.collection().id,value.text,value.mode);this.changed=true;await this.reload();this.snack.open('ورود واژه‌ها انجام شد.','باشه',{duration:2200})}async editEntry(entry:LibraryEntry|null=null){const value=await firstValueFrom(this.dialogs.open(LibraryEntryDialogComponent,{data:{entry}}).afterClosed()) as Record<string,unknown>|undefined;if(!value)return;if(entry)await this.api.updateEntry(this.collection().id,entry.id,value);else await this.api.addEntry(this.collection().id,value);this.changed=true;await this.reload()}async removeEntry(entry:LibraryEntry){const ok=await firstValueFrom(this.dialogs.open(ConfirmDialogComponent,{data:{title:'حذف واژه',message:'این واژه از مجموعه حذف شود؟',confirmLabel:'حذف',danger:true}}).afterClosed());if(!ok)return;await this.api.removeEntry(this.collection().id,entry.id);this.changed=true;await this.reload()}}
@Component({selector:'app-library-page',imports:[ReactiveFormsModule,MatButtonModule,MatCardModule,MatChipsModule,MatFormFieldModule,MatInputModule,MatProgressBarModule,MatSelectModule],template:`<section class="page"><header><div><h1>کتابخانه</h1><p>{{collections().length}} مجموعه · {{totalWords()}} واژه</p></div>@if(canManage()){<button mat-flat-button (click)="createCollection()">مجموعهٔ جدید</button>}</header><div class="stats"><mat-card appearance="outlined"><strong>{{collections().length}}</strong><span>مجموعه</span></mat-card><mat-card appearance="outlined"><strong>{{totalWords()}}</strong><span>کل واژه‌ها</span></mat-card><mat-card appearance="outlined"><strong>{{subscribedWords()}}</strong><span>واژه‌های مجموعه‌های من</span></mat-card></div><div class="filters"><mat-form-field appearance="outline"><mat-label>جستجو</mat-label><input matInput [formControl]="search"></mat-form-field><mat-form-field appearance="outline"><mat-label>نوع</mat-label><mat-select [formControl]="kind"><mat-option value="all">همه</mat-option><mat-option value="book">کتاب</mat-option><mat-option value="exam">آزمون</mat-option><mat-option value="topic">موضوعی</mat-option><mat-option value="course">دوره</mat-option></mat-select></mat-form-field><mat-form-field appearance="outline"><mat-label>وضعیت</mat-label><mat-select [formControl]="status"><mat-option value="all">همه</mat-option><mat-option value="subscribed">اضافه‌شده</mat-option><mat-option value="available">اضافه‌نشده</mat-option></mat-select></mat-form-field></div><div class="cards">@for(c of filtered();track c.id){<mat-card appearance="outlined"><mat-card-header><mat-card-title>{{c.title}}</mat-card-title><mat-card-subtitle>{{kindLabel(c.kind)}} · {{level(c)}} · نسخه {{c.contentVersion}}</mat-card-subtitle></mat-card-header><mat-card-content><p>{{c.description||'مجموعه‌ای از واژه‌ها برای مسیر یادگیری تو.'}}</p><div class="count"><span>{{c.wordCount}} واژه</span><strong>{{progress(c).percent}}٪ وارد لایتنر</strong></div><mat-progress-bar mode="determinate" [value]="progress(c).percent"/></mat-card-content><mat-card-actions><button mat-flat-button (click)="toggle(c)">{{c.subscribed?'✓ اضافه شده':'افزودن به جعبه'}}</button><button mat-button (click)="open(c)">مشاهده واژه‌ها</button>@if(canManage()){<button mat-icon-button (click)="edit(c)" title="ویرایش">✎</button>}</mat-card-actions></mat-card>}@empty{<p class="empty">مجموعه‌ای با این فیلتر پیدا نشد.</p>}</div></section>`,styles:[`:host{display:block}.page{display:grid;gap:18px}header{display:flex;justify-content:space-between;align-items:center}.stats{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.stats mat-card{display:grid;padding:16px}.stats strong{font-size:28px}.filters{display:grid;grid-template-columns:2fr 1fr 1fr;gap:10px}.cards{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:14px}.cards mat-card{border-radius:22px}.cards mat-card-content{display:grid;gap:10px}.count{display:flex;justify-content:space-between;font-size:12px}.empty{grid-column:1/-1;text-align:center;padding:40px}@media(max-width:1000px){.cards{grid-template-columns:1fr 1fr}}@media(max-width:700px){.stats,.filters,.cards{grid-template-columns:1fr}header{align-items:stretch;flex-direction:column}}`],changeDetection:ChangeDetectionStrategy.OnPush})export class LibraryPageComponent implements OnInit{private readonly api=inject(LibraryApiService);private readonly dialog=inject(MatDialog);private readonly snack=inject(MatSnackBar);readonly collections=signal<LibraryCollection[]>([]);readonly canManage=signal(false);readonly search=new FormControl('',{nonNullable:true});readonly kind=new FormControl('all',{nonNullable:true});readonly status=new FormControl('all',{nonNullable:true});readonly filtered=computed(()=>{const q=normalizeAnswer(this.search.value),kind=this.kind.value,status=this.status.value;return this.collections().filter(c=>(!q||normalizeAnswer(`${c.title} ${c.description||''} ${c.kind} ${levelOf(c)}`).includes(q))&&(kind==='all'||c.kind===kind)&&(status==='all'||(status==='subscribed'?c.subscribed:!c.subscribed))) });readonly totalWords=computed(()=>this.collections().reduce((n,c)=>n+c.wordCount,0));readonly subscribedWords=computed(()=>this.collections().filter(c=>c.subscribed).reduce((n,c)=>n+c.wordCount,0));level=levelOf;progress=progressOf;kindLabel(kind:string){return({book:'کتاب',exam:'آزمون',topic:'موضوعی',course:'دوره',personal:'شخصی'} as Record<string,string>)[kind]||'مجموعه'}async ngOnInit(){await this.load()}async load(){const result=await this.api.list();this.collections.set(result.collections||[]);this.canManage.set(Boolean(result.capabilities?.canManage))}async toggle(c:LibraryCollection){if(c.subscribed)await this.api.unsubscribe(c.id);else await this.api.subscribe(c.id);await this.load()}async open(c:LibraryCollection){const detail=await this.api.get(c.id);const changed=await firstValueFrom(this.dialog.open(LibraryDetailDialogComponent,{data:{collection:detail.collection,canManage:Boolean(detail.capabilities?.canManage)},maxWidth:'95vw'}).afterClosed());if(changed)await this.load()}async createCollection(){const value=await firstValueFrom(this.dialog.open(CollectionEditorComponent,{data:{collection:null}}).afterClosed()) as CollectionPayload|undefined;if(!value)return;const result=await this.api.create(value);await this.load();this.snack.open('مجموعه ساخته شد.','باشه',{duration:2000});await this.open(result.collection)}async edit(c:LibraryCollection){const value=await firstValueFrom(this.dialog.open(CollectionEditorComponent,{data:{collection:c}}).afterClosed()) as CollectionPayload|undefined;if(!value)return;await this.api.update(c.id,value);await this.load()}}
