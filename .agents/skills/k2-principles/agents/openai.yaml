interface:
  display_name: "K2 Principles"
  short_description: "Select relevant engineering principles at task start"
  default_prompt: "Use $k2-principles once before a mutation task. Select the smallest relevant principle set, read only those references, state why each applies, and return execution to the owning workflow."
