interface:
  display_name: "Vocora Design System"
  short_description: "Apply Vocora's complete light/dark visual design system."
  default_prompt: "Use $vocora-design-system before changing any Vocora visual surface. Treat light as the default theme and design light and dark variants together. Read the canonical design guide and tokens, reuse semantic tokens instead of feature-local colors, preserve accessibility and responsive behavior, and run the skill-owned validator/tests before finalizing visual-system changes."
